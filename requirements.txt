# Runtime dependencies of Edge-of-Chaos (P0_Distilled v0.1).
# Versions checked on 2026-09-22 (Python 3.12, Windows 11); tested in CI on
# Python 3.11 and 3.12 (Linux). For development/build (maturin, pyinstaller) see
# requirements-dev.txt.

numpy>=1.24,<3
scipy>=1.10,<2
matplotlib>=3.7,<4
seaborn>=0.12,<1
