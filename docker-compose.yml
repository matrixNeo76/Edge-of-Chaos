version: '3.8'

services:
  metrology-twin:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: edge_of_chaos_twin
    volumes:
      - ./output:/app/output
    environment:
      - PYTHONUNBUFFERED=1
    command: python3 valence_dashboard.py
